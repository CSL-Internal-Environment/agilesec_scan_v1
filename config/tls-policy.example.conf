# Secure TLS policy example for the remediated AgileSec demo.
min_protocol = TLSv1.2
preferred_protocol = TLSv1.3
cipher_policy = modern
certificate_source = managed-platform
private_key_source = kms-or-hsm

